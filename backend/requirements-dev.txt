# Test + lint tooling.
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
ruff>=0.5

# Lightweight runtime slice needed to import the app during tests
# (no Groq/Deepgram/Celery/Postgres required — the suite runs on SQLite).
fastapi
sqlalchemy
python-dotenv
itsdangerous
python-multipart
